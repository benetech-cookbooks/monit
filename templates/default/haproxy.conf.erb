check process haproxy with pidfile /var/run/haproxy.pid
    group database
    start program = "/etc/init.d/haproxy start" with timeout 120 seconds
    stop  program = "/etc/init.d/haproxy stop" with timeout 120 seconds
    if failed host localhost port 8000 protocol pgsql then restart
    if 10 restarts within 10 cycles then timeout